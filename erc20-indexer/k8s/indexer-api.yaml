apiVersion: apps/v1
kind: Deployment
metadata:
  name: indexer-api
  namespace: erc20
spec:
  replicas: 1
  selector:
    matchLabels: { app: indexer-api }
  template:
    metadata:
      labels: { app: indexer-api }
    spec:
      containers:
        - name: api
          image: abth21/indexer-api:v2
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          env:
            - name: PGHOST
              value: "postgres"
            - name: PGPORT
              value: "5432"
            - name: PGDATABASE
              value: "appdb"
            - name: PGUSER
              value: "app"
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: pg-secret
                  key: APP_PASSWORD
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: indexer-api
  namespace: erc20
spec:
  type: ClusterIP
  selector: { app: indexer-api }
  ports:
    - port: 80
      targetPort: 3000
